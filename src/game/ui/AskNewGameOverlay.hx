package game.ui;

import game.data.mutable.tweens.TweenedAlpha;
import h2d.Object;

@:build(macros.RecordClassMacro.Record.build())
class AskNewGameOverlay {

    var overlayContainer:Object;
    var textAlphaTween:TweenedAlpha;
}